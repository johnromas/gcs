<%= bootstrap_form_for(@attorney) do |f| %>
  <% if @attorney.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@attorney.errors.count, "error") %> prohibited this attorney from being saved:</h2>

      <ul>
      <% @attorney.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <%= f.text_field :first_name %>
  <%= f.text_field :last_name %>
  <%= f.text_field :address %>
  <%= f.text_field :city %>
  <%= f.collection_select(:state_id, State.all, :id, :name, {prompt: 'Select State'}) %>
  <%= f.text_field :zip %>
  <%= f.text_field :phone, class: "phone-input" %>
  <%= f.text_field :email %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
